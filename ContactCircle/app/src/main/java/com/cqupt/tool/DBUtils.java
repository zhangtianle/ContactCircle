package com.cqupt.tool;

import android.content.Context;

import com.cqupt.bean.Article;
import com.lidroid.xutils.DbUtils;
import com.lidroid.xutils.exception.DbException;

/**
 * Created by ls on 15-4-20.
 */
public class DBUtils {

    public static void saveDb(Context context) {
//        DbUtils.DaoConfig config = new DbUtils.DaoConfig(context);
//        config.setDbName("Message.db");
//        DbUtils.create(config);
//        DbUtils db = DbUtils.create(context);
//        Article message = new Article(); //这里需要注意的是User对象必须有id属性，或者有通过@ID注解的属性
//        message.setId(1);
//        message.setUserName("李双");
//        try {
//            db.save(message); // 使用saveBindingId保存实体时会为实体的id赋值
//        } catch (DbException e) {
//            e.printStackTrace();
//        }
    }
}
